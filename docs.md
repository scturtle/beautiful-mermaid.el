# beautiful-mermaid.el — 设计文档

> 本文档记录把 [beautiful-mermaid](https://github.com/lukilabs/beautiful-mermaid)
> (TypeScript)的 ASCII/Unicode 渲染器移植成单文件 Emacs Lisp 的设计:
> 范围取舍、架构映射、关键算法、字符策略、与上游的偏差、验证方法。
> 包布局:`beautiful-mermaid.el`(实现,~1900 行,零依赖,Emacs 26.1+)、
> `beautiful-mermaid-test.el`(55 个 E2E 测试)。
> **安装与用法见 [README.md](README.md)**。

---

## 1. 范围决策

原 TS 版支持 6 种图、15 套主题、ANSI 颜色、子图、样式指令……单文件
Elisp 版按"最小可用但质量不降级"取舍:

| 能力 | 决策 | 理由 |
|---|---|---|
| `graph`/`flowchart` 全方向(TD/TB/LR/RL/BT) | ✅ | 核心需求 |
| 全部 12 种节点形状 | ✅ | 都是"盒子 + 特殊角字符",只差一张查找表 |
| solid/dotted/thick 线、开线 `---` | ✅ | 只是换线字符(`─│` / `.:` / `═║`) |
| 边标签(两种语法)、双向箭头、链式、`A & B --> C & D`、自环、回边 | ✅ | 解析器 + A* 天然支持 |
| **边捆绑**(fan-in/fan-out 合并走线) | ✅ | 不捆绑平行边会绕路、箭头侧入,质量明显下降;逻辑仅 ~150 行 |
| 多行标签(`<br>`) | ✅ | 解析时替换为 `\n`,绘制时按行居中 |
| CJK 对齐 | ✅ **修正**(优于 TS) | TS 按 UTF-16 `length` 量宽会错位;本版按 `string-width` |
| 其他图类型(state/sequence/class/er/xychart) | ❌ 报错 | 各需独立布局器,超出最小范围 |
| subgraph、`classDef`/`style`/`linkStyle` | ❌ 静默忽略 | 子图需整套包围盒逻辑;样式在纯文本下无意义。忽略而非报错,共享源码仍可渲染 |
| 颜色 / 主题 / role canvas | ❌ | 终端纯文本输出 |
| SVG / ELK 布局 | ❌ | 另一条独立管线 |

---

## 2. 架构映射

TS 渲染管线(~3000 行,`src/parser.ts` + `src/ascii/` 15 个文件):

```
parseMermaid          正则行解析 → MermaidGraph
convertToAsciiGraph   → AsciiGraph(无子图时退化为直通映射)
createMapping         网格布局:根节点 → 逐层放子节点 → 列宽/行高
                      → 边捆绑分析 → A* 路由 → 标签定位 → 坐标转换
drawGraph             分层绘制:盒子 → 线 → 角 → 箭头 → 盒起点 → 标签
canvasToString        行主序拼接(BT 方向再整体垂直翻转)
```

函数映射(`bm--` 为内部前缀):

| TS 模块 | Elisp | 说明 |
|---|---|---|
| `parser.ts` | `bm--parse`, `bm--parse-edge-line`, `bm--consume-node(-group)` | 逐行正则;形状模式按最长定界符优先 |
| `converter.ts` | (内联) | 无子图/样式时为直通映射 |
| `grid.ts` | `bm--layout`, `bm--reserve`, `bm--set-col-width`, `bm--grid->drawing` | 布局编排 |
| `pathfinder.ts` | `bm--astar`, `bm--heap-create`, `bm--merge-path` | 二叉堆闭包 + 拐角惩罚启发式 |
| `edge-routing.ts` | `bm--determine-path`, `bm--start-and-end-dirs`, `bm--determine-label-line` | 起止方向选择表完整移植 |
| `edge-bundling.ts` | `bm--analyze-bundles`, `bm--process-bundles` | 仅 TD 方向生效(与 TS 一致) |
| `shapes/*` | `bm--shape-frame`, `bm--shape-grid-dims` | 角字符表 + 3 个特例尺寸 |
| `draw.ts` | `bm--draw-graph` + 分层绘制函数 | 层序与 TS 完全一致 |
| `canvas.ts` | `bm--canvas-put`, `bm--canvas-to-string`, `bm--flip-canvas` | 哈希画布 + 合并语义 |
| —(无对应) | org 集成 | src 块 overlay 切换 + org-babel,本包新增 |

**数据结构**:节点/边/图用 `cl-defstruct` 向量结构体;网格占用、列宽/行高、
画布全是哈希表(键 `(x . y)` 或整数),天然稀疏,画布无需预分配尺寸——
TS 的 `canvas[x][y]` 二维数组和 `increaseSize`/`mergeCanvases` 搬运逻辑被
整体替换。哈希表无序,凡依赖文档顺序(节点序、边序、束内首边)另配列表。

---

## 3. 关键算法

### 3.1 3×3 块布局

- 每个节点占逻辑网格一个 3×3 块(列 `[边框, 内容, 边框]`,行同理),块间由
  填充列/行(`bm-padding-x/y`,默认 5)隔开。
- 根节点 = 文档顺序中首次出现且未作为边终点的节点;放第 0 层,位置指针 +4。
  子节点逐层放置(`childLevel = parent ± 4`,同层用哈希表记录下一个空位),
  多轮扫描直到全部落位(容忍任意声明顺序)。
- 冲突时按垂直于主流方向 +4 平移重试(`bm--reserve`)。
- 列宽/行高取同列最大值——**同一列的盒子等宽**,这是 TS 输出的显著特征。
- 特例尺寸:`subroutine`/`stadium` 边框列宽 2(容纳 `╠╣`/`( )` 角),
  `cylinder` 边框行高 2(容纳圆弧)。

### 3.2 网格坐标 → 字符坐标

```
drawing(x,y) = Σ colWidth[0..x-1] + ⌊colWidth[x]/2⌋    (行同理)
```

即"单元格中心"。盒子左上角 = 3×3 块左上格的中心;块内 8 个方向常量
(`bm--up = (1 . 0)` 等,**块内偏移而非单位向量**)用于选取附件点,
恰好落在盒子边框中点。关键表:`bm--start-and-end-dirs` 按 from/to 相对
方向 × 图方向给出首选/备选起止附件方向,A* 各跑一次取短者。
边线绘制首尾各让 1 格:盒起点字符(`┬┴├┤`)画在源边框、箭头画在目标
边框外 1 格、拐角画在弯点——三层字符互不覆盖,这正是分层绘制的意义。

### 3.3 A* 寻路

- 4 向移动(独立单位向量表),目标格即使被占用也可进入。
- 启发式 = 曼哈顿 + 1(双轴非零时),偏好直线。
- 优先队列:closure 捕获的可增长向量二叉堆;`bm--merge-path` 折叠共线点。
- 首选/备选双路径都失败时退化为两点直连,箭头总有落点。

### 3.4 交叉线合并

两条边经过同一格:先画的 `─` 与后画的 `│` 合成 `┼`。TS 用 90 项查表;
本版改为**位掩码并集**——每个框线字符代表一组连接方向(左=1 上=2 右=4
下=8),合并 = `logior` 后反查。11 个字符的掩码表与全部多比特掩码
一一对应(双射),故与 TS 查表在所有可达输入上等价,代码仅 ~20 行。
另附加一条 TS 没有的小改进:`═`×`║` 合并为 `╬`。

### 3.5 边捆绑

仅 TD 方向生效(LR 下拐角自然合并,与 TS 一致):

- **fan-in**(多源同汇):汇合点在目标块上方 1 格,各源单独 A* 到汇合点,
  汇合点 → 目标共享一段 + 单个箭头。
- **fan-out**(同源多汇):分裂点在源块下方 1 格。
- 成束条件:≥2 条边、同线型、无标签、非自环。
- 汇合/分裂点按实际连接方向集合选字符(`┼┬┴├┤`…)。
- 共享段、汇合字符、fan-in 箭头只在束的首边画一次。

### 3.6 BT 方向 = TD 布局 + 整幅翻转

`RL` 按 TS 处理为 LR;`BT` 按 TD 走完整管线后把画布按行翻转,重映射
有向字符(`↑↔↓`、`┌↔└`、`┬↔┴`、`╔↔╚`、`↖↔↙` 等)。

### 3.7 CJK 宽字符(对 TS 的修正)

量宽用 `string-width`(显示列);绘制逐字符按显示宽度推进,宽字符把
覆盖到的画布单元标记为 `bm--wide-marker`,序列化时跳过——宽字形在
终端天然占两列,输出中相邻排布即连续显示,边框精确对齐:

```
┌──────┐     ┌──────────┐
│ 开始 ├────→│ 处理过程 │
└──────┘     └──────────┘
```

---

## 4. 字符集策略:`safe` / `full` 双档案

约束来自开发时所用字体的覆盖表(166 码点):表内可用单线/双线框全套、
细箭头 `←↑→↓↖↗↘↙`、实心三角 `▲▼◀▶`、`◊◌╱╲▷`;表外有
`╭╮╰╯ ┄┆ ━┃ ►◄ ◤◥◣◢ ◇◯◎ ╟╢ ⌜⌝⌞⌟`。

| 元素 | `safe`(默认) | `full`(TS 原版) |
|---|---|---|
| rounded / cylinder 角 | `╔╗╚╝` + `═║` 边 | `╭╮╰╯` + `─│` 边 |
| dotted 线 | `. :` | `┄ ┆` |
| thick 线 | `═ ║`,拐角 `╔╗╚╝` | `━ ┃`,拐角 `┌┐└┘` |
| diamond / circle / doublecircle | `◊` / `◌` / `◌` | `◇` / `◯` / `◎` |
| hexagon 角 | `╱╲╲╱` | `⌜⌝⌞⌟` |
| subroutine 角 | `╠╣` | `╟╢` |
| 箭头(两档案一致,默认实心) | `▶◀▲▼`+对角 `↖↗↘↙`;`bm-arrow-style='arrow` 时全细 `→←↑↓↖↗↘↙` | 同左 |

要点:箭头不进 `full` 档案——TS 的 `►◄`、`◤◥◣◢` 不在字体表内。替换
保持实心族:►→`▶`、◄→`◀`(`▲▼` 本就是表内字符,原样保留),仅对角
`◤◥◣◢`→`↖↗↘↙`(表内无实心对角);故默认箭头风格为实心三角,
`arrow` 选项才全用细线箭头(8 向全套统一)。`safe` 输出恒等于 `full` 的纯字符替换(几何一致),两档案共用全部
布局/绘制代码,只有 4 张查找表不同;`full` 是回归测试基线(对 TS 输出做
8 项箭头归一化后可逐字节比对)。

---

## 5. Elisp 移植要点(踩坑记录)

1. **正则方言反转**:`\\(` 是分组、`(` 是字面量(与 JS 相反);`\|` 是或;
   锚是 `\\``/`\\'`。
2. **非贪婪是 `*?`**;`\\{1,\\}?` 不是——会退化为贪婪,标签吞到最后一个
   `]`。所有形状模式用 `\\(.*?\\)`。
3. **`cl-defstruct` 槽位缺失运行时才爆**(`(setf bm--edge-xxx)` 无效)——
   先定全结构再写逻辑。
4. **块内方向 ≠ 移动向量**:`(1 . 0)` 是 3×3 块内偏移,A* 必须用独立单位
   向量表,混用会得到"只能往右下走"的寻路 → 死循环。
5. **哈希表无序**:依赖顺序的地方(节点序、边序、束内首边)配列表。
6. **docstring 内引号要 `\"`**:错一处字符串提前终止,`load` 可能碰巧
   平衡通过,byte-compile 警告才暴露——始终跑 `batch-byte-compile` 检查。
7. **性能**:16 节点图含 Emacs 启动共 66ms;A* 用真二叉堆,哈希画布
   稀疏存储免预分配。

---

## 6. 与 TS 的有意偏差(除字符替换外)

| 行为 | TS | Elisp 版 | 理由 |
|---|---|---|---|
| `<b>` 等格式标签 | 渲染为字面 `<b>bold</b>` | 剥离 | `<b>` 在终端是噪音 |
| CJK 标签 | 按 codepoint 计宽,错位 | 按 `string-width`,对齐 | 见 §3.7 |
| `graph LR; A --> B` | 报错(header 必须独占一行) | 接受 | 上游 README 示例即此写法 |
| 空标签 `A[]` | 不匹配,退化为裸节点 | 匹配为空标签 | 宽容 |

---

## 7. 验证

**黄金对拍**:27 个用例,覆盖 12 形状、四线型、两种标签语法、双向/开线、
链/环/自环/回边、三种线型的捆绑、标签阻止捆绑、`flowchart` 关键字、
节点组、多行标签、LR 边标签、跨级边、多根不连通子图、孤立节点、
深树碰撞平移、BT/RL 方向:

```
TS 渲染器 → golden 输出 → 箭头归一化(►→▶ ◄→◀ ◤◥◣◢→↖↗↘↙,§4)
  ↔ Elisp 渲染(full 档案 + 实心三角)→ 逐字节 diff
```

**结果:27/27 字节级一致**;safe 档案 27/27 无错渲染;两个文件
`batch-byte-compile` 零警告。

**回归锁**:27 个 golden + 1 个 CJK(safe 档案)作为字符串常量嵌入
测试套件,任何布局/路由/绘制变化都会打破对应测试。

测试套件共 55 个 ERT 测试,四组,全部端到端——只从公开 API
(`beautiful-mermaid-render`、交互命令、org 集成)驱动,只对最终输出
断言,不触碰内部函数;重构内部实现只要输出不变,测试不受影响:

| 组 | 数量 | 内容 |
|---|---|---|
| A. Rendering | 19 | 方向、等宽、BT 翻转不变式、捆绑计数、CJK、字符档案扫描、配置项 |
| B. Goldens | 28 | 27 个 TS 对拍 + CJK,字节级回归锁 |
| C. API | 3 | 返回值约定、region/buffer 命令 |
| D. Org | 5 | overlay 切换、批量切换、缩进对齐、babel 往返 |

```
emacs -Q --batch -l beautiful-mermaid-test.el -f ert-run-tests-batch-and-exit
```

---

## 8. 若要继续扩展

按依赖顺序:子图(包围盒 + 穿边路由)→ stateDiagram(复用本管线 +
伪状态形状)→ RL 真支持(水平翻转)→ ANSI 颜色 → sequence/class/er
(各自独立布局器,与 flowchart 管线无关)。
